# Dataset Notes

- Preferred source: Hugging Face `WenyangHui/Conic10K`.
- Hugging Face observed metadata on 2026-06-23: format `parquet`, license `mit`, dataset size 10K-100K, latest visible commit `69332d1`.
- Fallback source: official public repository `whyNLP/Conic10K`; observed files are `conic10k/train.json`, `conic10k/dev.json`, and `conic10k/test.json`.
- Official repository license: MIT.
- Raw data is not committed; `data/raw/` is ignored.
- In this execution environment, outbound package/data downloads from shell failed with `CONNECT tunnel failed, response 403`, so the committed report remains a template until the scripts are run in an environment with dataset access.
